export enum Accent {
  Primary = 'Primary',
  Quaternary = 'Quaternary',
  Secondary = 'Secondary',
  Tertiary = 'Tertiary',
}

export enum Key {
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight',
  ArrowUp = 'ArrowUp',
  End = 'End',
  Enter = 'Enter',
  Escape = 'Escape',
  Home = 'Home',
  P = 'p',
  R = 'r',
  Space = ' ',
}
